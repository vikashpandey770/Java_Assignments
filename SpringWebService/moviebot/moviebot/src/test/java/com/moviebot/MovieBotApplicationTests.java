package com.moviebot;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class MovieBotApplicationTests {

    @Test
    void contextLoads() {
        // Verifies the Spring Boot application context starts up correctly
        // with all beans (controller, service, WebClient) wired properly.
    }
}
